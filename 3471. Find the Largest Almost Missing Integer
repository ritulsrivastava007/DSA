class Solution {
    public int largestInteger(int[] nums, int k) {
        int n = nums.length;
        int ans = -1;
        for (int i = 0; i < n; i++) {
            int x = nums[i];
            int count = 0;
            for (int start = 0; start <= n - k; start++) {
                for (int j = start; j < start + k; j++) {
                    if (nums[j] == x) {
                        count++;
                        break;
                    }
                }
            }
            if (count == 1) {
                ans = Math.max(ans, x);
            }
        }
        return ans;
    }
}
